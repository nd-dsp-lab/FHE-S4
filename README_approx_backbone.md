# Approximate S4D Backbone

The canonical handoff materials now live in `approx_backbone/`.

Start here:
- `approx_backbone/README.md`
- `approx_backbone/model.py`
- `approx_backbone/eval_backbone.py`

Quick check:

```bash
python eval_approx_backbone.py
```

or

```bash
python -m approx_backbone.eval_backbone
```
